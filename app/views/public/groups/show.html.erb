<div style = "height:95vh;",  id = "comment-full w-100">
  <% if @group.end_users.include?(current_end_user) %>
    <div class = "w-100 h-75 border overflow-auto", id = "group_comment_container">
    <% if @group_comments.blank? %>
      <h3>
        コメントを打ち込むとここに表示されます
      </h3>
    <% end %>
      <%= render @group_comments %>
    </div>
  <% else %>
    <h3>同時視聴のコメントを観覧、投稿したい場合は、グループに参加してください</h3>
  <% end %>
  <% if @group.watching? %>
    <% if @current_end_user.email != "guest@example.com" %>
      <%= render "group_comment_form", group:@group, group_comment:@group_comment %>
    <% end %>
    <h3>視聴中</h3>
  <% elsif @group.waiting? %>
    <h3>
      <%= @group.start_time.strftime('%Y/%m/%d %H:%M') %>から視聴開始
    </h3>
  <% elsif @group.closed? %>
    <h3>同時視聴は終了しました</h3>
  <% end %>
</div>