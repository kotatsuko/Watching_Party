  <%= form_with model: [group, group_comment],local: false do |f| %>
    <%= f.text_area :body, size: "60x3", id: "group_comment_content", placeholder: "コメントをここに" %>
    <%= f.submit "送信する", id: 'comment-button' %>
  <% end %>